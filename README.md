# dlchan-releases

Release binaries and update manifest for [DL-chan](https://github.com/shiawasenanami/dlchan).

`latest.json` is polled by the app's in-app update checker — don't rename or restructure it without also updating `src/main/updateChecker.js` in the main repo.
